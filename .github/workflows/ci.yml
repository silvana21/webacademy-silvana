
name: Primeiro Workflow

on:
    push:

jobs:

    primeiro-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v5
            - name: ler Readme
              run: cat README.md

